pub mod defaults {
    pub const TITLE: &str = "RuPy";
    pub const SIZE_SMALL: (u32, u32) = (400, 300);
    pub const SIZE_MEDIUM: (u32, u32) = (800, 600);
    pub const SIZE_LARGE: (u32, u32) = (1280, 720);
}
