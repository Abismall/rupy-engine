pub mod descriptor;
pub mod text;
#[derive(Debug)]

pub struct MenuComponentHash(pub u32);
// #[derive(Debug)]

// pub enum MenuComponent {
//     Button(Id, Button),
//     TextField(Id, TextField),
//     Text(Id, text::Text),
// }
